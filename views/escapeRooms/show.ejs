<body class="login">
<header class="main" id="mainHeader">

    <div class="right">
        <% if (!session.user) { %>
            <a href="/">Login</a>
        <% } else { %>
            <%= session.user.username %>

        <% } %>
    </div>
    <div class="left">
        <% if (session.user) { %>
        <a href="/?_method=DELETE">Logout</a>
        <% } %>
    </div>

</header>
<h1>
    Configuración básica de la escape room:
</h1>

<br class="escapeRoomItem">

<div>
    <% var attachment = escapeRoom.attachment; %>
    <% include ../attachments/_show %>
</div>
    <div>
        <br />
        Título: <b><%= escapeRoom.title %></b>
        <br />
        Profesor/a: <b><%= escapeRoom.teacher %></b>
        <br />
        Asignatura: <b><%= escapeRoom.subject %></b>
        <br />
        Duración: <b><%= escapeRoom.duration %></b>
        <br />
        Descripción: <b><%= escapeRoom.description %></b>
        <br />
        Vídeo: <b><%= escapeRoom.video %></b>
        <br />
        Número máximo de participantes: <b><%= escapeRoom.nmax %></b>
        <br />
        Inivitación: <b><%= escapeRoom.invitation %></b>
        <br />
    </div>
    </br>

</div>

<h2>
    Turnos:
</h2>

<% var turnos = escapeRoom.turnos; %>

<% include ../turnos/index %>
</br>

<h2>
    Apariencia:
</h2>
<script>
    $(function getTema(){
        const tema=document.getElementById("appearance").value;
    });
</script>


</br>
<td>
    <a href="/escapeRooms/<%= escapeRoom.id %>/edit"><button class="editButton">editar</button></a>
</td>
<td>
    <a href="/escapeRooms/<%= escapeRoom.id %>?_method=DELETE"
       onClick="return confirm('Delete: <%= escapeRoom.title %>');">
        <a href="/users/<%= session.user.id %>/escapeRooms"><button class="deleteButton">borrar</button></a>
    </a>
</td>

<a href="/users/<%= session.user.id %>/escapeRooms"><button>Volver</button></a>

<footer>
    <img src="/images/logo.png" width="10%" style=" margin-top: 100px"/>
    <img src="/images/ESCUDO_UPM.png" width="5%" style=" margin-top: 100px; float: right"/>
</footer>

</body>